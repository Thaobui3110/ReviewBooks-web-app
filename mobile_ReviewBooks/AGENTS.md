# Expo HAS CHANGED

This project is pinned to Expo SDK 54 (downgraded from the create-expo-app default). Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code — do not assume SDK 57 or "latest" APIs.
